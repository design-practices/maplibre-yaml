# Workstream E assertion: the CLI must validate (exit 0) a root pages: document.
pages:
  - path: "/"
    title: "Home"
    blocks:
      - type: map
        id: home-map
        config:
          center: [-74.006, 40.7128]
          zoom: 10
          mapStyle: "https://demotiles.maplibre.org/style.json"
        layers: []
